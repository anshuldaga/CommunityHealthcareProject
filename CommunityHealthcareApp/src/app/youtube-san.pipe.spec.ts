import { YoutubeSanPipe } from './youtube-san.pipe';

describe('YoutubeSanPipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubeSanPipe();
    expect(pipe).toBeTruthy();
  });
});
